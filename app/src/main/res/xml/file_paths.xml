<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Allow sharing files from cache directory -->
    <cache-path
        name="cache"
        path="." />

    <!-- Allow sharing files from external files/Documents -->
    <external-files-path
        name="external_files"
        path="." />
</paths>